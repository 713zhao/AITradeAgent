[tool:pytest]
testpaths = tests
addopts =
    --cov=finance_service
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
    -v
    --tb=short
python_files = test_*.py
python_classes = Test*
python_functions = test_*
